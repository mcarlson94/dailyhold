## Packages
framer-motion | Animations for timer and page transitions
lucide-react | Icons for UI elements (check, share, x, etc.)
canvas-confetti | Celebration effect on completion
date-fns | Date manipulation for countdowns

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  display: ["var(--font-display)"],
  body: ["var(--font-body)"],
  mono: ["var(--font-mono)"],
}
Extend colors:
colors: {
  brand: {
    bg: "#d4dfe6",
    primary: "#2d5446",
    secondary: "#3e6b5a",
    accent: "#ffffff",
  }
}
